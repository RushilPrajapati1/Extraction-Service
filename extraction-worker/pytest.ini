[pytest]
testpaths = tests
# tests/ imports the pipeline modules flatly (`import storage`), matching
# how the app itself runs. Without this, only conftest.py's sys.path hack
# would make that work.
pythonpath = .
addopts = -q
